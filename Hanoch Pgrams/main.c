#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a=0,b=0,sum=0;
    printf("Give first number -");
    scanf("%d",&a);
    printf("Give second number -");
    scanf("%d",&b);

    sum = a+b;
    printf("The sum is %d\n",sum);

    return 0;
}
